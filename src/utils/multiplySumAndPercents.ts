/*
    I created this method because I had some thoughts about 0.03 of percent and 0.03 percents compare...
*/

export const multiplySumAndPercents = (percents: number, sum: number) => {
    return (percents / 100) * sum;
};
